name: HP更新（シート→本番反映）

on:
  schedule:
    - cron: '*/5 * * * *'      # 5分ごと（GitHubの都合で数分前後する場合あり）
  workflow_dispatch:            # 手動実行（「今すぐ反映」）
  repository_dispatch:          # 外部トリガー（将来のGASボタン用）
    types: [hp-update]

permissions:
  contents: write

concurrency:
  group: hp-update
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    env:
      # 社員用シート（公開読取CSV）
      SHEET_CSV:   "https://docs.google.com/spreadsheets/d/13Laq5NAwrDUbWVp-fDljj34vP_vIXQobFyr0hpLvKqQ/export?format=csv&gid=0"
      ARTICLE_CSV: "https://docs.google.com/spreadsheets/d/13Laq5NAwrDUbWVp-fDljj34vP_vIXQobFyr0hpLvKqQ/export?format=csv&gid=2"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 新規記事を生成（記事追加タブ）
        run: python3 tools/add_article.py --csv "$ARTICLE_CSV"

      - name: 既存情報をHTMLへ反映（HP更新タブ）
        run: python3 tools/overlay_build.py --csv "$SHEET_CSV"

      - name: 変更があればコミット＆公開
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git config user.name  "yorimichi-bot"
            git config user.email "bot@users.noreply.github.com"
            git add -A
            git commit -m "HP更新: シート反映 ($(date -u +%Y-%m-%dT%H:%MZ))"
            git push
            echo "✅ 反映しました"
          else
            echo "変更なし（反映済み）"
          fi
